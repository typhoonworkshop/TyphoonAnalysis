{
  "dependencies": {
    "@observablehq/notebook-kit": "^2.1.4"
  },
  "scripts": {
    "preview": "notebooks preview --root nbks",
    "build": "notebooks build --root nbks --out ../dist -- nbks/*.html && cp nbks/typhoonGB.txt ../dist/typhoonGB.txt"
  },
  "devDependencies": {
    "esbuild": "^0.28.0"
  }
}
